# 단권화 — 작업 기록

2026년 9월 11일, 한 세션 동안 시안부터 배포까지 만든 기록.
무엇을 왜 그렇게 정했는지, 어디서 막혔고 뭘 잘못했는지까지 남긴다.

---

## 주소

| | |
|---|---|
| 웹앱 (공개) | https://kimjinsol049-pixel.github.io/dangwonhwa/ |
| 화면 시안 | https://kimjinsol049-pixel.github.io/dangwonhwa/design.html |
| 저장소 | https://github.com/kimjinsol049-pixel/dangwonhwa |
| Claude 앱판 | https://claude.ai/code/artifact/045a8bd2-95f6-4a08-83de-afd32e397526 |
| Vercel | 미배포 — `DEPLOY.md` 참고 |

---

## 만든 것

강의 필기든 교재 발췌든 넣으면 **단원별 정리본**을 만들고, 그 정리본 내용으로
**문제를 내고 채점**해서 **약한 단원**을 모아주는 웹앱.

빌드 도구 없는 HTML 한 파일이다. 프레임워크도 번들러도 없고, 같은 코드가
Claude 아티팩트 안과 일반 웹 양쪽에서 돈다.

---

## 시작할 때 정한 것

만들기 전에 물어서 받은 답. 추측한 게 아니라 전부 사용자가 고른 것이다.

| 질문 | 정한 것 |
|---|---|
| 산출물 | 시안을 먼저 확정하고 그 다음 동작하는 앱 (2단계) |
| 자료 입력 | 텍스트 + PDF + 사진 전부 |
| 저장·공유 | 혼합 — 자료·정리본·문제는 공유, 성적은 개인 |
| 취약점 기준 | 단원별 정답률 (인지수준 분류 아님) |
| 시험 종류 | 여러 종류 섞임 → 과목 타입을 안 가리는 범용 구조 |
| 문제 유형 | 객관식 + OX + 단답 |

---

## 진행 순서

### 1. 화면 방향 고르기

문제 풀이 화면을 **세 방향**으로 그려서 고르게 했다. 취향이 아니라
*무엇을 화면의 주인공으로 둘 것인가*가 달랐다.

- **A 노트** — 한국 시험지 조판. 명조 지문, 얇은 괘선, ①~⑤, 빨간 배점
- **B 집중** — 잉크블루 배경에 문제 하나, 타이머, 숫자키
- **C 분석** — 왼쪽 정리본 근거 / 가운데 문제 / 오른쪽 단원별 정답률

**고른 답: A 구조 + 답 확인 순간만 C처럼 + 취약점은 홈으로.**

이때 방향이 한 번 크게 바뀌었다 — "자료정리를 해주고 문제는 원할 때".
문제 풀이가 아니라 **정리본이 본진**이 되고, 문제는 정리본 단원 끝에서
기다리는 옆방이 됐다.

### 2. 나머지 화면

고른 방향으로 여덟 화면을 그렸다. 홈 · 자료 추가 · 정리본 · 모드 고르기 ·
연습(문제/답 확인) · 실전(풀이/결과).

여기서 요청 두 개가 더 붙었다:

- **풀이 모드 두 가지** — 연습(즉시 채점)과 실전(타이머 + OMR 답안지 + 몰아서 채점)
- **라이트/다크** — 색을 뒤집지 않고 다시 골랐다. 따뜻한 먹색 바탕에 크림색 글자,
  "램프 밑에 놓인 종이"

### 3. 동작하는 앱

시안을 실제로 돌아가게 만들었다. 원래 계획은 "동작"과 "저장"을 나누는 것이었는데,
저장을 나중에 붙이면 데이터 구조를 두 번 짓게 돼서 합쳤다.

### 4. PDF · 사진 올리기

- **사진** — AI에 이미지를 직접 넘긴다. 여러 장이면 묶음으로 나눠 순차 처리
- **PDF** — 브라우저에서 pdf.js로 텍스트를 먼저 뽑고, 글자가 없는 스캔본이면
  페이지를 이미지로 렌더해 사진과 같은 경로로

### 5. 실패 원인 표시

"정리를 못한다"는 신고를 받고 오류를 드러내게 고쳤다.
→ **진짜 원인은 내 오타였다.** 아래 "고친 버그" 참고.

### 6. 함께 쓰기

공유는 이미 되고 있었는데 앱 안에서 그걸 알 수가 없었다. 공유 안내, 이름 설정,
"함께 쓰는 사람" 목록, 약한 단원의 **나 / 다 같이** 토글을 넣었다.

### 7. 깃허브 + 공개 주소

GitHub Pages로 공개 주소를 만들었다. 같은 파일이 Claude 안팎에서 돌도록,
`window.claude`가 없으면 여는 사람의 API 키를 쓰는 경로를 넣었다.

### 8. 무료 AI

"키 없이 무료로"라는 요청에 두 갈래를 넣었다.

- **브라우저에서 직접** — WebLLM + WebGPU. 모델을 한 번 내려받아 기기 안에서 돌린다.
  키도 서버도 없고 자료가 밖으로 안 나간다
- **Gemini 무료 티어** — 조사해보니 구글은 카드 없이 상시 무료 티어를 준다.
  OpenAI는 2025년에 무료 크레딧이 끊겨서 사실상 불가

### 9. 이상한 문제 걸러내기

`"고체, 액체, 기체, 플라즈마의 특징을 설명하는 문장은?"` 이 **OX 문제**로 나왔다는
신고. O/X로 답할 수 없는 지문이다. 아래 "고친 버그" 참고.

### 10. 과목 전체 풀기

단원 하나가 아니라 과목 전체를 한 번에 풀 수 있게 했다. 이미 만들어둔 단원 문제를
먼저 쓰고 없는 단원만 새로 만든 뒤, **단원을 돌아가며 한 문제씩** 뽑아 섞는다.

### 11. Vercel 서버리스 (배포 대기)

방문자가 키를 안 넣어도 되게 `api/ai.js`를 만들었다. 소유자의 키로 AI를 대신 부른다.
키는 Vercel 환경변수에만 있고 브라우저로 안 내려간다.

---

## 지금 구조

### 파일

| 파일 | 용도 |
|---|---|
| `index.html` | 웹에서 여는 완성본 (`artifact.html`을 문서로 감싼 것) |
| `artifact.html` | Claude 아티팩트 원본 조각 — **고칠 땐 이걸 고친다** |
| `api/ai.js` | Vercel 서버리스 AI 중계 |
| `design.html` | 화면 시안 여덟 장 |
| `DEPLOY.md` | Vercel 배포 절차 |

`index.html`은 `artifact.html`에서 생성한다. 수정하면 **아티팩트 퍼블리시**와
**리포 재빌드·푸시**를 둘 다 해야 한다.

### 저장 (Claude 아티팩트에서는 서버, 그 밖에서는 브라우저)

```
subjects/{id}                       과목
subjects/{id}/sections/{secId}      정리본 단원 (본문·핵심지점·표)
subjects/{id}/qsets/{secId}         그 단원의 문제집
subjects/{id}/materials/{matId}     올린 자료 기록
progress/{learnerId}                이름 + 푼 기록
```

`makeStore()`가 서버(db)와 localStorage 중 하나를 고른다. 쓰는 쪽 코드는 같다.

### AI 경로 네 가지

실행 위치를 보고 알아서 고른다.

| 경로 | 쓰이는 곳 | 키 | 사진 | 한 번에 넣는 자료 |
|---|---|---|---|---|
| Claude 계정 | Claude 아티팩트 안 | 불필요 | ○ | 7,000자 |
| 서버 제공 | Vercel 배포판 | 불필요 | ○ | 7,000자 |
| Gemini | 웹 | 무료 키 | ○ | 7,000자 |
| 브라우저 모델 | 웹 | 불필요 | ✕ | 1,800자 |
| Claude API | 웹 | 유료 키 | ○ | 7,000자 |

### 출제 품질 장치

1. 프롬프트에 유형별 지문 형태를 예시와 함께 못박음
2. `usableQuestion()`이 저장 전에 걸러냄 — 질문형 OX, 보기 중복·부족 객관식,
   답이 40자 넘는 단답
3. 그래도 새면 **"이 문제 이상해"** 버튼으로 직접 제거 (문제집과 성적 기록에서 함께 삭제)
4. 답이 길이 제한에 걸려 잘려도 온전히 닫힌 단원만 골라 건짐

---

## 부딪힌 제약

| 제약 | 어떻게 했나 |
|---|---|
| 이 PC에 node·bun·python 전부 없음 | `/design` 캔버스 대신 일반 아티팩트로 그림. 로컬 서버·Vercel CLI 포기 |
| `user` 권한이 계정에 없음 | 개인별 비공개 경로를 못 씀 → 브라우저별 학습자 ID로 성적을 가름 (엄밀한 비공개는 아님, 사용자에게 고지) |
| 서버 저장을 쓰면 조직 내부용이 됨 | 공개 링크 불가 → 공개용은 GitHub Pages / Vercel로 분리 |
| 아티팩트는 외부 통신이 막힘 | 무료 모델·Gemini는 Claude 밖에서만 동작 |
| 브라우저 모델 문맥이 4096 토큰 | 그 경로만 입력 한도를 1,800자로 낮춤 |
| Gemini 모델명이 문서마다 다름 | 하드코딩 안 함 — 키를 넣는 순간 API에 목록을 직접 조회 |
| git "Filename too long" | 작업 경로가 너무 길어서 짧은 임시 경로에서 작업 |

---

## 고친 버그

### 따옴표 오타 하나로 앱 전체가 죽음 — 가장 큰 실수

문자열을 `'`로 열고 `"`로 닫았다. 스크립트 전체가 파싱에 실패해 **빈 화면**이 됐다.

나쁜 건 그걸 모른 채 **세 번을 더 배포**했고, "정리를 못한다"는 신고를 받고도
엉뚱하게 오류 표시 기능을 만들고 있었다는 것이다 — 그 코드조차 파싱이 안 돼
실행된 적이 없었다. 브라우저에서 한 번만 열어봤으면 즉시 잡혔을 일이다.

**이후로는** 배포할 때마다 브라우저에서 파싱을 확인한다. 인라인 스크립트는 줄 번호가
안 나와서, 스크립트를 잘라 Blob으로 주입해 `error` 이벤트에서 위치를 받는 방법을 쓴다.

### OX 문제가 질문형으로 나옴

`"~설명하는 문장은?"`은 객관식 지문이지 OX가 아니다. 프롬프트가 "answer는 true/false"만
말하고 **지문이 서술문이어야 한다**는 걸 안 짚었다. 프롬프트 + 필터 + 수동 제거 세 겹으로 막았다.

### 그 밖

- 실전 채점에서 문항마다 저장을 때려 속도 제한에 걸릴 수 있었음 → 한 번에 저장
- confirm 모달 분기를 제목 문자열로 비교하고 있었음 → `kind` 필드로
- 시안 단계에서 정리본·답확인 화면이 프레임 높이를 넘어 잘림 → 높이 재계산 + 내부 스크롤
- `sed`가 JS 문자열 안에 코드를 그대로 박아넣음 → 직접 수정

---

## 남은 것

- **정리본 PDF 내려받기** — 처음 요청한 7가지 중 유일하게 안 된 것
- **Vercel 배포** — 코드는 준비됐고 대시보드에서 연결만 하면 됨
- 단답 채점이 문자열 비교라 표기가 다르면 틀린 걸로 나옴 ("이거 맞은 걸로"로 정정 가능)
- 공유가 앱 단위라 과목별로 따로 공유할 수 없음
- 웹 공개판은 여러 사람이 내용을 함께 보는 기능이 없음
